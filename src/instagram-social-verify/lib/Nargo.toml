[package]
name = "instagram_sv"
type = "lib"
authors = ["Siz <email@domain>"]
compiler_version = "^1.0.0"
license = "MIT"


[dependencies]
zkemail      = { tag = "v0.4.2", git = "https://github.com/zkemail/zkemail.nr", directory = "lib" }   # bu repo hâlen lib/ kullanıyor
noir_bignum  = { tag = "v0.6.0", git = "https://github.com/noir-lang/noir-bignum" }


[tool.nargo]
# Bu seçenekler yalnızca paket derlenirken geçerli:
compile_tests = false      # bağımlılıkların tests klasörlerini atla

[workspace]
compile_tests = false

